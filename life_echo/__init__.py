"""LifeEcho emotional continuity engine.

LifeEcho is deliberately kept as a standalone package inside the repository so it
can run natively inside IndiCare now and later be exposed as a plugin/API layer
for other care, education, safeguarding and health systems.
"""

__all__ = ["router"]

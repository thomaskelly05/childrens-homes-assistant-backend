(function(){
  if(window.__IndiCareAIStreamingInstalled) return;
  window.__IndiCareAIStreamingInstalled=true;

  function mode(){return localStorage.getItem('ic.ai.mode')||document.querySelector('.ai-rail nav button.active')?.dataset?.mode||'assistant';}
  function esc(v){return String(v||'').replace(/[&<>"']/g,function(c){return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c];});}
  function feed(){return document.querySelector('.voice-transcript,.ai-feed');}
  function ensureLiveBubble(){var f=feed();if(!f)return null;var row=document.getElementById('icLiveStreamingAnswer');if(row)return row.querySelector('p');row=document.createElement('article');row.id='icLiveStreamingAnswer';row.className='ai-message assistant live-streaming';row.innerHTML='<div>AI</div><p></p>';f.appendChild(row);f.scrollTop=f.scrollHeight;return row.querySelector('p');}
  function renderSources(meta){var f=feed();if(!f||!meta)return;var sources=(meta.sources||meta.assistant_context?.orchestrator_sources||[]).filter(Boolean).slice(0,8);var explain=meta.explainability||{};var scope=meta.assistant_scope||{};if(!sources.length&&!Object.keys(explain).length&&!Object.keys(scope).length)return;var existing=document.getElementById('icEvidencePanel');if(existing)existing.remove();var panel=document.createElement('section');panel.id='icEvidencePanel';panel.className='evidence-panel';panel.innerHTML='<h3>Knowledge used</h3>'+(sources.length?'<div class="source-chips">'+sources.map(function(s,i){var label=s.title||s.name||s.source||s.label||('Source '+(i+1));return '<button title="'+esc(s.url||s.path||label)+'">'+esc(label)+'</button>';}).join('')+'</div>':'<p>Assistant used live orchestrated context where available.</p>')+'<details><summary>Why this answer</summary><pre>'+esc(JSON.stringify({scope:scope,explainability:explain},null,2))+'</pre></details>';f.appendChild(panel);f.scrollTop=f.scrollHeight;}
  function parseSse(buffer,onToken,onMeta){var parts=buffer.split('\n\n');var rest=parts.pop()||'';parts.forEach(function(part){var event='message';var data=[];part.split('\n').forEach(function(line){if(line.indexOf('event:')===0)event=line.slice(6).trim();if(line.indexOf('data:')===0)data.push(line.slice(5).trim());});var text=data.join('\n');if(!text||text==='[DONE]')return;if(event==='meta'){try{onMeta&&onMeta(JSON.parse(text));}catch(e){}}else if(event==='progress'){window.dispatchEvent(new CustomEvent('indicare:stream-progress',{detail:{content:text}}));}else{onToken(text);}});return rest;}
  async function stream(body){var activeMode=mode();var outbound=Object.assign({},body||{},{assistant_surface:'ai-suite',assistant_mode:activeMode,response_mode:activeMode==='intelligence'?'deep':(body&&body.response_mode)||'balanced',conversation_id:body&&body.conversation_id||('indicare-ai-'+activeMode),use_orchestrator:true});var bubble=ensureLiveBubble();var answer='';var meta=null;window.dispatchEvent(new CustomEvent('indicare:stream-start',{detail:{mode:activeMode}}));var res=await originalFetch('/assistant/general/stream',{method:'POST',credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify(outbound)});if(!res.ok||!res.body)throw new Error('Streaming failed '+res.status);var reader=res.body.getReader();var decoder=new TextDecoder();var buffer='';while(true){var next=await reader.read();if(next.done)break;buffer+=decoder.decode(next.value,{stream:true});buffer=parseSse(buffer,function(token){answer+=token;if(bubble){bubble.textContent=answer;var f=bubble.closest('.ai-feed,.voice-transcript');if(f)f.scrollTop=f.scrollHeight;}window.dispatchEvent(new CustomEvent('indicare:stream-token',{detail:{token:token,answer:answer,mode:activeMode}}));},function(m){meta=m;});}var live=document.getElementById('icLiveStreamingAnswer');if(live)live.remove();renderSources(meta);if(window.IndiCareAI&&window.IndiCareAI.remember)window.IndiCareAI.remember('answer',answer,{mode:activeMode,meta:meta});if(activeMode==='intelligence'&&window.IndiCareVoice&&window.IndiCareVoice.speak)window.IndiCareVoice.speak(answer);window.dispatchEvent(new CustomEvent('indicare:stream-done',{detail:{answer:answer,meta:meta,mode:activeMode}}));return{answer:answer.trim()||'I am here. Tell me what you want to work through.',meta:meta};}
  var originalFetch=window.fetch.bind(window);
  window.fetch=async function(url,opts){if(typeof url==='string'&&url.indexOf('/assistant/general-safe')>-1&&opts&&opts.method==='POST'){try{var body=JSON.parse(opts.body||'{}');var data=await stream(body);return new Response(JSON.stringify(data),{status:200,headers:{'Content-Type':'application/json'}});}catch(error){return new Response(JSON.stringify({answer:'I could not connect to the live assistant stream just now. '+(error.message||error)}),{status:200,headers:{'Content-Type':'application/json'}});}}return originalFetch(url,opts);};
  window.IndiCareAIStream={stream:stream};
})();

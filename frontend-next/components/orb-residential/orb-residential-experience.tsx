/**
 * Legacy entry — canonical shell lives in `@/components/orb/orb-shell`.
 * Intentionally retained so existing imports and structure tests keep working.
 */
export { OrbShell as OrbResidentialExperience } from '@/components/orb/orb-shell'

# ORB Residential legacy UI archive

Historical CSS from Phase 1D–1I and pre-1K stacked design systems.

These files are **not imported** into the active `/orb` render path.
The single canonical design system is `app/orb/orb-residential-shell.css`.
